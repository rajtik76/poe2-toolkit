{
  "name": "poe2-toolkit",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-agnostic toolkit for Path of Exile 2 data: a shared GGPK access layer plus extractors and renderers for the passive tree, items, gems and runes. Code only - ships no game data or art.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @poe2-toolkit/ggpk && npm run build -w @poe2-toolkit/tree-core && npm run build -w @poe2-toolkit/tree-react && npm run build -w @poe2-toolkit/tree-extractor && npm run build -w @poe2-toolkit/item-extractor && npm run build -w @poe2-toolkit/gem-extractor && npm run build -w @poe2-toolkit/rune-extractor && npm run build -w @poe2-toolkit/mod-extractor",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:coverage": "npm run test --workspaces --if-present -- --coverage",
    "lint": "eslint packages",
    "check:readme": "node scripts/check-readme-packages.mjs",
    "ci": "npm run build && npm run typecheck && npm run lint && npm run check:readme && npm run test:coverage",
    "fixtures:extract": "node scripts/golden-fixtures/setup.mjs",
    "fixtures:bless": "node scripts/golden-fixtures/setup.mjs --bless"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@stylistic/eslint-plugin": "^5.10.0",
    "@vitest/coverage-v8": "^3.2.7",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^10.0.1",
    "eslint-import-resolver-typescript": "^4.4.4",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-react": "^7.37.3",
    "eslint-plugin-react-hooks": "^7.0.0",
    "globals": "^15.14.0",
    "pathofexile-dat": "^15.2.0",
    "pathofexile-dat-schema": "^8.0.0",
    "typescript-eslint": "^8.23.0"
  },
  "engines": {
    "node": ">=18"
  }
}
